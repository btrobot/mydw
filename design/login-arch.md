
    ┌─────────────┐     ┌─────────────┐     ┌─────────────┐                                                                            │   前端      │     │   SSE       │     │   后端      │                                                                            │ LoginModal  │────▶│  Stream     │◀────│ dewu_client │                                                                            │             │     │             │     │             │
    │ - 发送验证码 │     │ - 状态推送   │     │ - Playwright│
    │ - 填写验证码│     │ - 进度更新   │     │ - 自动化操作 │
    │ - 状态显示  │     │             │     │             │
    └─────────────┘     └─────────────┘     └─────────────┘
          │                                       │
          │ POST /api/accounts/login/{id}         │
          └───────────────────────────────────────┘

    数据流

    1. 用户在前端输入手机号 → 点击获取验证码
    2. 前端 POST 请求到后端
    3. 后端 Playwright 自动化打开得物网页
    4. SSE 推送状态给前端实时显示
    5. 用户输入验证码 → 提交
    6. 后端完成登录 → 保存 storage_state

