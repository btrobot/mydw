# 远端下一步建议

日期：2026-04-26

## 结论

当前最合理的下一步是：

**直接进入下一独立切片：Admin destructive step-up runtime。**

原因：

1. 主线迁移已经完成
2. Alembic / 限流 / 分页 / Argon2 已完成
3. Day 5.6 已把 step-up 方案收敛清楚，剩下的是把设计变成最小运行时闭环

## 建议执行顺序

### Step 1：后端 schema + helper

- 新增 `admin_step_up_grants`
- 新增 `hash_step_up_token / verify_step_up_token`
- 不复用账户密码 helper

### Step 2：新增 step-up verify 入口

- `POST /admin/step-up/password/verify`
- 输入管理员当前密码
- 返回短时 step-up token

### Step 3：接入 destructive admin route

- `users revoke / restore`
- `devices unbind / disable / rebind`
- `sessions revoke`
- `PATCH /admin/users/{user_id}` 中敏感字段更新

### Step 4：强制校验放进 service

- 在 `admin_service` 内收口
- 不只在 route 层判断 header

### Step 5：测试与审计

- contract test
- service test
- migration test
- audit event
- step-up rate limit

## 不建议现在做的事

- 不建议回头再做大范围前端迁移
- 不建议把 TOTP 一起做掉
- 不建议把 end-user self-service step-up 一起拉进来

## 一句话

**下一刀不要再铺新面，直接把“admin 高危操作 step-up”做成一个可独立验收的后端安全切片。**
