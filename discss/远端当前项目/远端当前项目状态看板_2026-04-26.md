# 远端当前项目状态看板（汇报版 / 工程版双栏）

日期：2026-04-26  
范围：`remote/` 工作区（`remote-backend` / `remote-admin` / `remote-shared`）

---

## 总览

| 维度 | 汇报版 | 工程版 |
| --- | --- | --- |
| 当前阶段 | **主线已完成，进入增强/收口期** | 原 Slice 0 ~ Slice 11 已完成；当前重点转向测试入口统一、文档化、少量工程优化 |
| 当前结论 | **已具备可演示、可回归、可继续增量开发的基础** | React 新壳、关键 auth contract、Alembic baseline、Argon2 升级、admin step-up security 已闭环 |
| 当前风险中心 | **不是主功能缺失，而是后续维护与回归一致性** | 主要风险已从“迁移失败”转为“测试矩阵维护、smoke 稳定性、构建体积与文档入口分散” |

---

## 一、状态双栏

| 主题 | 汇报版 | 工程版 |
| --- | --- | --- |
| 后端主线 | **完成** | FastAPI remote backend 已补齐 admin / self-service 关键 contract 回归；destructive API 的 `401/403/404/429` 最小入口已覆盖 |
| 前端主线 | **完成** | `remote-admin` 已以 React 新壳为主入口；login / dashboard / users / devices / sessions / audit logs 已迁移完成 |
| 安全主线 | **完成** | admin destructive flow 已接入 `step-up verify`；后端 schema / route / service enforcement 与 `X-Step-Up-Token` 合同已贯通 |
| 数据库演进 | **完成** | Alembic baseline 已落地；schema 变更入口已统一到迁移体系 |
| 密码升级 | **完成** | Argon2 平滑升级已完成：双栈校验 + opportunistic rehash |
| 用户管理 | **可用且有真实回归** | Users Create / Update / Revoke / Restore 已打通，并已有真实浏览器 smoke 与一键入口 |
| 设备/会话管理 | **可用** | Devices / Sessions 列表、详情、destructive flow、refresh 策略已完成迁移和回归 |
| 审计能力 | **可用** | Audit logs 页面已迁移，基础 loading / empty / error / filter 行为已覆盖 |
| 启动与入口 | **已收敛** | `scripts/start-remote.bat` 默认打开 React 主入口，避免旧壳/新壳错位 |
| 术语与文案 | **已收口一轮** | 页面标题、README、启动提示、空状态/错误状态/Danger zone 文案、部分中英文术语已统一 |

---

## 二、已完成项

### 汇报版

- Remote React 管理端迁移完成
- 关键 auth / contract 回归完成
- 分页与 list shape 统一完成
- Alembic baseline 完成
- Argon2 平滑升级完成
- admin step-up security 完成
- Users 真实烟测入口完成收口

### 工程版

1. **后端 / contract**
   - `/refresh`、`/logout`、`/me`、self-service 关键 route 最小 contract 回归已补
   - admin destructive API 最小 contract 回归已补
   - admin / self-service 列表返回 shape 已统一为：
     - `items`
     - `total`
     - `page`
     - `page_size`
   - 登录限流替换方案已落地，优先保持前端合同不变

2. **前端 / Remote Admin**
   - React 主入口生效：`dist-react/index.html?apiBase=http://127.0.0.1:8100`
   - route guard / 登录恢复 / 列表-详情 refresh 策略已补稳
   - destructive action 已统一接入 `useAdminStepUp`
   - Users / Devices / Sessions / Audit Logs 页面已完成迁移

3. **安全 / runtime**
   - admin step-up verify runtime 已落地
   - destructive 请求 token header 已贯通
   - React 级 step-up 回归与关键浏览器差异回归已补

4. **真实烟测入口**
   - `npm run smoke:users:create`
   - `npm run smoke:users:update`
   - `npm run smoke:users:update:multi`
   - `npm run smoke:users`
   - `smoke:users` 已支持按 step-up 限流窗口自动等待，避免整套连跑撞 `429`

---

## 三、当前验证能力

| 维度 | 汇报版 | 工程版 |
| --- | --- | --- |
| 基础构建验证 | **已具备** | `typecheck` / `build` / `build:react` 可直接运行 |
| 前端契约验证 | **已具备** | `npm --prefix remote/remote-admin run test` |
| step-up 交互验证 | **已具备** | `npm --prefix remote/remote-admin run test:react-step-up` |
| 用户真实烟测 | **已具备** | `npm --prefix remote/remote-admin run smoke:users` |
| 发布门禁基础 | **已具备** | `remote/README.md` 与 `remote-shared/docs/phase4-release-gate.md` 已给出门禁序列 |

---

## 四、当前未完成项性质判断

| 维度 | 汇报版 | 工程版 |
| --- | --- | --- |
| 主功能缺口 | **无主线缺口** | 当前不是“功能没做完”，而是“主线已完成后的增强 backlog” |
| 正在进行中的核心 slice | **无** | 原计划口径下已无进行中的核心 slice |
| 未开始的主线项 | **无** | 后续事项主要是测试说明、差异分支补强、构建优化、测试依赖收敛 |

---

## 五、后续 backlog

### 汇报版

1. 补测试覆盖说明与维护文档  
2. 继续补 step-up 差异失败分支回归  
3. 做少量工程化优化（测试依赖、构建体积、测试模板）

### 工程版

#### P0

- 输出 remote-admin 测试覆盖说明 / 维护文档
- 输出 step-up 浏览器回归维护说明
- 收敛当前分散的状态/看板/总结入口

#### P1

- 细化 restore / unbind / rebind 的 failure / token_expired 回归
- 评估是否把 remote-admin 测试依赖进一步内聚到子项目
- 抽 destructive admin route 接线模板

#### P2

- Vite chunk / 构建体积优化
- 更细粒度 visual / UX 回归
- browser harness / helper 再抽象

---

## 六、当前风险与注意事项

| 汇报版 | 工程版 |
| --- | --- |
| 当前风险主要在“后续维护一致性”，不在“主功能不可用” | `smoke:users` 当前依赖 suite runner 中对 step-up 次数的预算；若后续 smoke 新增确认动作，需要同步更新 `suiteSteps` |
| 系统已进入可持续演进阶段 | 工作区仍存在一些与当前 slice 无关的脏文件 / 未跟踪文件，后续提交仍需保持隔离 |
| 后续工作应以增强与工程化为主 | `vite build` 仍有大 chunk warning，但当前不阻塞功能与回归 |

---

## 七、推荐对外口径

### 汇报版

> Remote 这一轮最小风险改造主线已经完成：React 管理端迁移、关键 auth/contract 回归、分页与返回 shape 统一、Alembic baseline、Argon2 平滑升级、admin step-up security 都已落地。当前阶段不再是补主功能，而是继续做测试入口统一、回归增强和工程化收口。

### 工程版

> 当前 `remote/` 工作区已经从“迁移实施期”进入“增强/收口期”。后端 contract、前端 React 新壳、数据库迁移基线、安全加固与关键浏览器级回归均已闭环。后续优先级应从新增主功能转向：测试矩阵透明化、step-up 差异分支补强、测试依赖边界收敛与构建优化。

