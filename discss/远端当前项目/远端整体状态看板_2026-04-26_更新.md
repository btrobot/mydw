# 远端整体状态看板（更新于 2026-04-26）

## 总览

按当前这轮最小风险改造主线（Slice 0 ~ Slice 11）来看：

- **已完成：12**
- **进行中：0**
- **未开始：0**

如果把 Day 5.7 ~ Day 5.11 的 admin step-up runtime + frontend + browser regression 视为 Slice 11 的后半段，那么它也已经完成闭环。

---

## 已完成

| Slice | 状态 | 当前结论 |
| --- | --- | --- |
| Slice 0 | 已完成 | 现状行为基线、迁移验收清单、最小风险路线图已产出 |
| Slice 1 | 已完成 | 后端 service/test 基础补稳 |
| Slice 2 | 已完成 | RBAC 与关键 API 最小集成测试入口已补齐 |
| Slice 3 | 已完成 | remote-admin React 新壳、route guard、核心迁移骨架已落地 |
| Slice 4 | 已完成 | login、dashboard、关键 auth contract / rate-limit / refresh / logout / me / self-service 回归已补齐 |
| Slice 5 | 已完成 | users 页迁移完成，revoke / restore + list/detail refresh 已闭环 |
| Slice 6 | 已完成 | devices、sessions 页迁移完成，destructive flow + refresh 已闭环 |
| Slice 7 | 已完成 | audit logs 页迁移完成 |
| Slice 8 | 已完成 | 登录限流替换方案与 destructive / auth route contract 最小集成回归已落地 |
| Slice 9 | 已完成 | admin + self-service 列表 shape 与分页消费已统一 |
| Slice 10 | 已完成 | Alembic baseline / 唯一迁移入口已落地 |
| Slice 11 | 已完成 | Argon2 平滑升级 + admin step-up security（runtime / frontend / regression）已完成 |

### Slice 11 完成内容（当前应视为已收口）

- Day 5.4：Argon2 升级设计
- Day 5.5：双栈校验 + opportunistic rehash
- Day 5.6：step-up security 设计
- Day 5.7：后端 step-up runtime 落地
  - schema / route / service enforcement / header contract
- Day 5.8：admin 前端 destructive flow 接上 step-up verify + token header
- Day 5.9：users revoke React 级 step-up 浏览器回归
- Day 5.10：devices disable + sessions revoke 浏览器回归
- Day 5.11：users restore + devices unbind/rebind 差异浏览器回归

---

## 进行中

**无**

当前没有仍处于“半完成”的主线 slice。

---

## 未开始

### 按原主线 Slice 口径

**无**

### 如果按“后续增强项”口径

以下内容可以视为下一轮增强 backlog，而不是本轮主线未完成项：

1. 补一份 remote-admin 测试覆盖矩阵/维护说明
2. 为 step-up browser suite 再补更细颗粒度失败矩阵（如 restore / unbind / rebind 的 failure / token_expired）
3. 评估是否把 remote-admin 的浏览器测试依赖完全内聚到子项目自身
4. 进一步做 chunk 拆分/构建体积治理（当前 `vite build` 有大包 warning，但不阻塞功能与回归）

---

## 一句话状态

当前这轮远端改造主线可以视为：

- **前端迁移主线：已完成**
- **后端 contract / route / service 回归：已完成**
- **分页 / 形状统一 / Alembic 基础设施：已完成**
- **Argon2 平滑升级：已完成**
- **admin step-up security：已完成**

也就是说，**原计划主线已经全部做完，现在进入的是“增强项/收尾优化”阶段**。
