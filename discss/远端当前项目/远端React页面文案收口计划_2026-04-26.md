# 远端 React 页面文案收口计划（2026-04-26）

## 本次目标
- 去掉 Remote Admin React 页面里残留的 `Day x`、`迁移中`、`React xxx list` 这类交付过程口径。
- 保持当前功能、交互、测试入口不变，只收口用户可见文案。

## 已盘点到的主要问题
- `DashboardPage.tsx`
  - `Migration status`
  - `Day 4 ... production-hardening ...`
- `UsersPage.tsx`
  - `Day 3 starts ... migration ...`
  - `during and after migration`
  - `The React users list ...`
- `DevicesPage.tsx`
  - `Day 3.2 migrates ...`
  - `The React devices list ...`
- `SessionsPage.tsx`
  - `Day 3.3 migrates ...`
  - `The React sessions list ...`
- `AuditLogsPage.tsx`
  - `Day 3.4 migrates ...`
  - `The React audit investigation list ...`

## 收口原则
1. 面向最终产品使用者，而不是面向迭代过程。
2. 保留有价值的能力说明：
   - 只读 / 可写角色差异
   - step-up 二次确认
   - 后端驱动刷新
   - 审计与调查语义
3. 不扩大范围：
   - 不改运行时
   - 不改接口合同
   - 不改页面结构
   - 不顺手做新的文案体系重写

## 预期改法
- `Day x / migration` → 改成能力说明
- `React xxx list` → 改成 `... could not be loaded from the admin API`
- `Migration status` → 改成更长期的状态文案，如 `Operational status`

## 验收标准
- `remote/remote-admin/src/pages/**` 中不再出现用户可见的 `Day x` / `migration` 口径
- 构建与现有 React 回归通过

## 实施结果
- 已收口页面：
  - `DashboardPage.tsx`
  - `UsersPage.tsx`
  - `DevicesPage.tsx`
  - `SessionsPage.tsx`
  - `AuditLogsPage.tsx`
- 已完成的文案调整：
  - `Day x` / `migration` 过程口径 → 产品能力说明
  - `Write-capable role detected` / `Read-only role detected` → 更自然的访问能力表述
  - `The React ... list ...` → `... could not be loaded from the admin API`
  - `Migration status` → `Operational status`

## 本次验证
- `npm --prefix remote/remote-admin run typecheck`
- `npm --prefix remote/remote-admin run build:react`
- `npm --prefix remote/remote-admin test`
- `npm --prefix remote/remote-admin run test:react-step-up`

## 当前结论
- 页面已从“迭代中说明”收口到“产品态能力说明”。
- 当前剩余告警仍是既有的 Vite chunk size warning，与本次文案改动无关。
